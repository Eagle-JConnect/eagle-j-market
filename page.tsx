'use client';import {useState} from 'react';import {Plus,Search,MoreHorizontal,Megaphone} from 'lucide-react';import {DashboardShell} from '@/components/DashboardShell';
const initial=[{name:'Summer Leads',channel:'Meta Ads',status:'Active',budget:'$6,240',leads:486,roas:'4.8x'},{name:'Brand Awareness',channel:'Instagram',status:'Active',budget:'$3,810',leads:294,roas:'3.9x'},{name:'Local Search',channel:'Google Ads',status:'Active',budget:'$4,620',leads:318,roas:'4.2x'},{name:'Holiday Retargeting',channel:'Meta Ads',status:'Paused',budget:'$1,950',leads:150,roas:'2.7x'}];
export default function Campaigns(){const [campaigns,setCampaigns]=useState(initial);const [q,setQ]=useState('');const add=()=>setCampaigns(c=>[{name:'New Growth Campaign',channel:'Meta Ads',status:'Active',budget:'$0',leads:0,roas:'—'},...c]);return <DashboardShell title="Campaigns"><div className="mb-6 flex flex-wrap items-end justify-between gap-4"><div><h2 className="text-3xl font-black">Campaigns</h2><p className="mt-1 text-slate-500">Create, monitor and optimize your marketing campaigns.</p></div><button onClick={add} className="flex items-center gap-2 rounded-xl bg-eagle-600 px-5 py-3 text-sm font-bold text-white"><Plus size={17}/> New campaign</button></div><div className="rounded-2xl border border-slate-200 bg-white"><div className="border-b p-4"><div className="relative max-w-sm"><Search className="absolute left-3 top-3 text-slate-400" size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search campaigns..." className="w-full rounded-xl border border-slate-200 py-2.5 pl-10 pr-3 outline-none focus:border-green-500"/></div></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-5 py-4">Campaign</th><th>Channel</th><th>Status</th><th>Budget</th><th>Leads</th><th>ROAS</th><th/></tr></thead><tbody className="divide-y">{campaigns.filter(x=>x.name.toLowerCase().includes(q.toLowerCase())).map(c=><tr key={c.name} className="hover:bg-slate-50"><td className="px-5 py-4"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-lg bg-green-50 text-green-700"><Megaphone size={16}/></div><b>{c.name}</b></div></td><td>{c.channel}</td><td><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${c.status==='Active'?'bg-green-100 text-green-700':'bg-slate-100 text-slate-500'}`}>{c.status}</span></td><td>{c.budget}</td><td>{c.leads}</td><td className="font-bold">{c.roas}</td><td><MoreHorizontal size={18}/></td></tr>)}</tbody></table></div></div></DashboardShell>}
