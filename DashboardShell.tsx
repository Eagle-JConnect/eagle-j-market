'use client';
import Link from 'next/link';import {usePathname} from 'next/navigation';import {LayoutDashboard,Megaphone,Users,BarChart3,Briefcase,Settings,LogOut,Menu,X} from 'lucide-react';import {useState} from 'react';import {Logo} from './Logo';
const items=[['/dashboard','Dashboard',LayoutDashboard],['/campaigns','Campaigns',Megaphone],['/clients','Clients & Leads',Users],['/analytics','Analytics',BarChart3],['/services','Services',Briefcase],['/settings','Settings',Settings]] as const;
export function DashboardShell({children,title}:{children:React.ReactNode,title:string}){const path=usePathname();const [open,setOpen]=useState(false);return <div className="min-h-screen bg-[#f6f9f7]"><aside className={`fixed inset-y-0 left-0 z-50 w-72 border-r border-slate-200 bg-white p-5 transition-transform ${open?'translate-x-0':'-translate-x-full'} lg:translate-x-0`}><div className="flex items-center justify-between"><Link href="/"><Logo/></Link><button className="lg:hidden" onClick={()=>setOpen(false)}><X/></button></div><nav className="mt-8 grid gap-2">{items.map(([href,label,Icon])=><Link onClick={()=>setOpen(false)} key={href} href={href} className={`flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-bold ${path===href?'bg-green-50 text-green-700':'text-slate-600 hover:bg-slate-50'}`}><Icon size={18}/>{label}</Link>)}</nav><div className="absolute bottom-5 left-5 right-5 rounded-2xl bg-slate-950 p-4 text-white"><p className="text-sm font-bold">Eagle-J Pro</p><p className="mt-1 text-xs text-slate-400">Growth workspace active</p></div></aside>{open&&<div onClick={()=>setOpen(false)} className="fixed inset-0 z-40 bg-black/30 lg:hidden"/>}<main className="lg:pl-72"><header className="sticky top-0 z-30 flex h-20 items-center justify-between border-b border-slate-200 bg-white/90 px-5 backdrop-blur md:px-8"><button className="lg:hidden" onClick={()=>setOpen(true)}><Menu/></button><div className="hidden lg:block"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Workspace</p><h1 className="text-xl font-black">{title}</h1></div><div className="ml-auto flex items-center gap-3"><div className="hidden text-right sm:block"><p className="text-sm font-bold">Judes Business</p><p className="text-xs text-slate-500">Owner</p></div><div className="grid h-10 w-10 place-items-center rounded-full bg-green-100 font-black text-green-800">JB</div></div></header><div className="p-5 md:p-8">{children}</div></main></div>}
